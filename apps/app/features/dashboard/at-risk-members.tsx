import { Card, CardHeader, CardTitle } from "@conquest/ui/card";

export const AtRiskMembers = () => {
  return (
    <Card className="flex-1">
      <CardHeader>
        <CardTitle>At Risk Members</CardTitle>
      </CardHeader>
    </Card>
  );
};
