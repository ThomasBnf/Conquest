{
  "name": "@conquest/prisma",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:seed": "prisma db seed"
  },
  "prisma": {
    "seed": "bun run prisma/seed.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
